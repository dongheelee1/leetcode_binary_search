Binary search is an algorithm that looks for a target value in an ORDERED array. 
After every loop/recursion, the algorithm cuts down the search space in half.  

Binary search won't work if the array is not ordered, so one must sort the array first. 
